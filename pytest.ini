[pytest]
testpaths = tests
# Without this, `pytest` from the project root cannot import `app` — pytest only
# puts the *test* directory on sys.path, not the project root. It happened to work
# on some setups and not others, which is the worst kind of failure.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
